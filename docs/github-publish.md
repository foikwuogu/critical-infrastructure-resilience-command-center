# GitHub

```bash
git init
git add .
git commit -m "Initial critical infrastructure resilience command center"
git branch -M main
git remote add origin https://github.com/YOUR-USERNAME/critical-infrastructure-resilience-command-center.git
git push -u origin main
```

Never commit credentials, proprietary OT data, real asset identifiers, SCADA configuration or production network information.
